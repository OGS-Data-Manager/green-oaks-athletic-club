# GOAC Handoff — 2026-07-16

*Session paused; Dave picks up tomorrow. The website is a working register-and-pay
shop; a few pieces are still pending — mostly blocked on a club Google login.*

## Where we are

The GOAC site (`greenoaksathleticclub.com`) was converted from a Google-Form +
Zelle signup into a **one-stop register-and-pay shop**. The full architecture,
rationale, and standing decisions are in memory: **`project_payments.md`**. The
Stripe/Sheet setup steps are in **`Docs for Huber/Stripe Registration Setup - 2026-07-16.md`**.
Don't re-derive those — read them.

## Done and LIVE (pushed to `main`, serving on greenoaksathleticclub.com)

- **Register & Pay — $120** button + chat widget wired to the real Stripe link
  `https://buy.stripe.com/8x2aEP30h7ULfDQ8A063K00`.
- **Stripe account** created. Dave Huber is an **Administrator**; **Dave Izenstark
  owns** the account (his email + 2FA). Payment Link product: "Green Oaks Athletic
  Club — K–2 Fall Session #1", $120, 50-payment cap, custom fields (child name /
  grade / allergies) + phone, ToS → `https://greenoaksathleticclub.com/waiver.html`.
- **`waiver.html`** live. Dave I reviewed it; refunds removed — **fee is
  non-refundable, no refunds for missed weeks** (§4).
- Earlier same-session polish: all contact links now email **both coaches**
  (`izenstark,callahan@ogschool.org`); weather FAQ + chat-widget answer hedge
  indoor/outdoor and the "rainy days" widget mis-routing bug is fixed.

Recent commits: `git log --oneline` (top ~8). Nothing uncommitted of substance
(only an untracked `.DS_Store`).

## PENDING — pick up here

1. **Waitlist Google Form** — the last 2 placeholders in `index.html`
   (`forms.gle/REPLACE_ME_WAITLIST`, ~lines 438 and 479). **Blocked:** the form
   should be owned by the club account **GreenOaksAthleticClub@gmail.com**, which
   **Izenstark owns but Dave doesn't have the login for yet** (getting it tomorrow).
   Form = just 2 fields (parent name + email). Once the `forms.gle/…` URL exists →
   swap both placeholders → commit → push.
2. **Roster for Cody** — Zapier/Make automation: Stripe "Checkout Session Completed"
   → append row to a Google Sheet Cody bookmarks. Cody never logs into Stripe.
   (Steps in the setup guide.)
3. **Club email** — DNS is on **Cloudflare** (nameservers `*.ns.cloudflare.com`),
   **no MX yet**. Recommendation already given: use the club Gmail as the base
   (owns Form/Sheet/Zapier), then add a **free Cloudflare Email Routing** branded
   address (`info@` or `coaches@greenoaksathleticclub.com`) forwarding into it.
   Skip paid mailboxes. Consider moving public contacts off `@ogschool.org` (school
   district email for a private LLC).

## Optional / nice-to-have

- **Test the flow before announcing:** either a real $120 purchase + refund (costs
  ~$3.78 Stripe fee, not returned on refund), or free via Stripe **Test mode** with
  card `4242 4242 4242 4242`. Offered to walk Dave through the test-mode version.
- Nobody has been told about the site yet, so live dead-ends (the waitlist link)
  harm nothing for now.

## Hosting facts (so the next session doesn't re-investigate)

- Repo: `github.com/EZPeasyDave/green-oaks-athletic-club` → GitHub Pages, fronted by
  Cloudflare at `greenoaksathleticclub.com`. Push → Pages rebuild → public in
  **~1–2 min**. Pages use `.html` (e.g. `/waiver.html`; extensionless `/waiver`
  404s — expected, not a bug).
- Single-file site: `index.html` (~570 lines; base64 fonts/images make it huge in
  bytes — use Edit, never Write, and read narrow line ranges). Chat widget is inline
  JS near the bottom; KB entries have `keywords`/`answer`/`follow`, matched by a
  simple `score()` function (multi-word keywords score higher — that's how the
  weather fix worked).

## Suggested skills next session

- **`/pipe`** for any read-and-review docs.
- No workflow/multi-agent tooling needed — this is small, sequential web edits.
